import DashboardSection from "@/components/dashboard";

export const metadata = {
  title: "Cashful test - Dashboard",
};

export default async function Dashboard() {
  return (
    <div>
      <DashboardSection />
    </div>
  );
}
